<?php

/**
 * German is the application's source language: the German strings live directly
 * in the code and views, so this map is intentionally empty. It exists only so
 * the locale list and file-loading logic treat 'de' like any other locale.
 *
 * Add entries here only to override a source string for German specifically.
 *
 * @return array<string,string>
 */
return [];
